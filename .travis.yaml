sudo: required
services:
  - docker
  
before_install:
  - docker build -t cristianchira/docker-react -f Dockerfile.dev .
   
script:
  - docker run cristianchira/docker-react npm run test -- --coverage   